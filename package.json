{
  "name": "companybrain",
  "version": "1.0.0",
  "description": "CompanyBrain Secure Enterprise AI Knowledge Platform",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && node src/server.js",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "test:security": "cd server && node test-security.js"
  }
}
